<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doan_final_2019">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.GioHangActivity"
            android:parentActivityName=".activities.SanPhamActivity"></activity>
        <activity
            android:name=".activities.SanPhamItemActivity"
            android:parentActivityName=".activities.SanPhamActivity" />
        <activity
            android:name=".activities.InfoActivity"
            android:label="Thông tin"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.NhanVienActivity"
            android:label="Nhân viên"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.DanhMucActivity"
            android:label="Danh Mục"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.SanPhamActivity"
            android:label="Sản phẩm"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Category">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>